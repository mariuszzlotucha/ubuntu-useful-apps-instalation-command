# Super-duper command
# With every clear ubuntu instalation I must go through all of those commands. It's f**king annoying

sudo apt install zsh -y  \
&& chsh -s $(which zsh)  \
&& sudo apt-get update -y \
&& sudo apt-get upgrade -y \
&& sudo apt autoremove -y \
&& sudo apt-get install powerline fonts-powerline git-core xclip virtualbox openssh-client curl libical-dev glib2.0 gitk fonts-symbola -y \
&& sudo apt install libdbus-1-dev libudev-dev libical-dev libreadline-dev pavucontrol net-tools rpi-imager terminator apt-transport-https ca-certificates curl gnupg-agent software-properties-common docker-ce docker-ce-cli containerd.io gdebi-core libxml2:i386 libcanberra-gtk-module:i386 gtk2-engines-murrine:i386 libatk-adaptor:i386 -y \
&& git clone https://github.com/robbyrussell/oh-my-zsh.git ~/.oh-my-zsh \
&& cp ~/.oh-my-zsh/templates/zshrc.zsh-template ~/.zshrc  \
&& chsh -s /bin/zsh \
&& cd .oh-my-zsh \
&& cd \
&& git clone https://github.com/zsh-users/zsh-syntax-highlighting.git "$HOME/.zsh-syntax-highlighting" --depth 1  \
&& echo "source $HOME/.zsh-syntax-highlighting/zsh-syntax-highlighting.zsh" >> "$HOME/.zshrc" \
&& echo 'alias update="sudo apt-get update -y && sudo apt-get upgrade -y && sudo apt autoclean -y && sudo apt autoremove -y"' >> ~/.zshrc \
&& source  ~/.zshrc \
&& update \
&& sudo snap install keepassxc caprine vlc  --classic code -y \
&& wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb \
&& sudo apt install ./google-chrome-stable_current_amd64.deb -y \
&& curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.35.3/install.sh | zsh \
&& rm google-chrome-stable_current_amd64.deb \
&& nvm install node \
&& npm install -g yarn \
&& yarn set version latest \
&& wget ftp://ftp.adobe.com/pub/adobe/reader/unix/9.x/9.5.5/enu/AdbeRdr9.5.5-1_i386linux_enu.deb \
&& sudo gdebi AdbeRdr9.5.5-1_i386linux_enu.deb \
&& rm AdbeRdr9.5.5-1_i386linux_enu.deb \
&& wget http://www.kernel.org/pub/linux/bluetooth/bluez-5.52.tar.xz \
&& tar -xf bluez-5.52.tar.xz \
&& cd bluez-5.52 \
&& ./configure \
&& sudo make install \
&& sudo systemctl daemon-reload  \
&& sudo apt-get install blueman \
&& sudo apt autoremove -y
&& update \ 
&& npm i -g gatsby \
&& curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add - \
&& sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" \
&& sudo usermod -aG docker $USER \
&& wget https://download.eset.com/com/eset/apps/home/eav/linux/latest/eset_nod32av_64bit_pl.linux \
&& sudo chmod +x eset_nod32av_64bit_pl.linux \
&& update \
&& echo "I'm done here"

# install and activate ES ET

# For browser integration with keepass you must run script from the official keepass page

# install from sofware center
skype
audacity
atom
screenshot
spotify

# set manually
nano ~/.zshrc # ZSH_THEME="agnoster" 
source  ~/.zshrc 

git config --global user.email "you@example.com"
git config --global user.name "Your Name"
 
ssh-keygen -t rsa -b 4096 -C "you@example.com"

xclip -selection clipboard < ~/.ssh/id_rsa.pub

